"use strict";

import jwt from 'jwt-simple';
//import * as secret from './utilities';
//import bcrypt from 'bcrypt';
//import user from './../database/users';

var add = (x, y) => { return x + y; };

console.log(add(10, 100));
